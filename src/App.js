import React from 'react';
import Deck from './Deck';

// The App component serves as the root component for this React application. 
// It renders the Deck component, which contains main functionality of the app. 
function App() {
  return (
    <div className="App">
      <Deck />
    </div>
  );
}

export default App;
